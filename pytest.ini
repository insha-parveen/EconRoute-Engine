[pytest]
# asyncio_mode = auto lets async test functions run without a per-test decorator
# (the @pytest.mark.asyncio markers we do add are harmless and self-documenting).
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
addopts = -q

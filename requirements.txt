# EconRoute — Python dependencies
# Install: pip install -r requirements.txt
# Versions use >= to allow compatible updates (pin with == for prod deployments)

# ─── API Framework ────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0          # ASGI server that runs FastAPI
pydantic>=2.7.0                    # data validation (FastAPI uses this internally)

# ─── LLM Gateway ─────────────────────────────────────────────────────────────
litellm>=1.40.0                    # unified interface to Groq, Ollama, OpenAI etc.
groq>=0.9.0                        # Groq SDK (LiteLLM uses this under the hood)

# ─── Semantic Cache (Week 2) ──────────────────────────────────────────────────
redis>=5.0.0                       # async Redis client
sentence-transformers>=3.0.0       # local embeddings (all-MiniLM-L6-v2)

# ─── Complexity Classifier (Week 3) ──────────────────────────────────────────
semantic-router>=0.0.72            # zero-shot local query routing

# ─── Database (Week 4) ────────────────────────────────────────────────────────
sqlalchemy>=2.0.0                  # async ORM
asyncpg>=0.29.0                    # async PostgreSQL driver (used by SQLAlchemy)
alembic>=1.13.0                    # DB migrations

# ─── Observability ───────────────────────────────────────────────────────────
prometheus-fastapi-instrumentator>=2.3.0   # auto-instruments FastAPI for Prometheus

# ─── Utilities ───────────────────────────────────────────────────────────────
python-dotenv>=1.0.0               # loads .env file
httpx>=0.27.0                      # async HTTP client (used by LiteLLM)
tenacity>=8.3.0                    # retry logic with exponential backoff

# ─── Dev/Test ─────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0             # async test support

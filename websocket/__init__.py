"""websocket package — Week 5 real-time request live feed (/ws/requests).

Import the singleton and helper directly from websocket.manager:
    from websocket.manager import manager, broadcast_request_event
"""
